"use strict" // treat all js code as new version

// alert(5+5) // this is work on browser not in node 

// eCMA Scripts Language

// number = 1

// BigInt

// Boolean = true and false

// string = ""

// null = standalone CSSMathValue

// undefined

// Symbol = unique

// Object

console.table([typeof(""), typeof(unique), typeof(A), typeof(true), typeof(123), typeof(null), typeof(undefined)])